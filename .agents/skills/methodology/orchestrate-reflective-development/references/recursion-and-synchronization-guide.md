# Recursion and Synchronization Guide

A finding may suggest an audit, specification, issue, or implementation,
but each transition requires its own authority.

Stop when a budget is reached, scope expansion is needed, architecture
is ambiguous, validation fails, review capacity is exceeded, or a
checkpoint is reached.

Speedrun mode groups work. It does not remove scope, validation, budget,
review, or approval.
