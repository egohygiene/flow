{
  "schema": "aether.skill-evaluations/v2",
  "skill": "github-issue-authoring",
  "version": "1.0.0",
  "cases": [
    {
      "id": "spec-to-issue",
      "description": "Evidence-backed requirements must become one copy-ready implementation issue.",
      "category": "positive",
      "trigger": "should-trigger",
      "expected": [
        "Extracts the real problem and desired outcome",
        "Produces scoped issue content with observable acceptance criteria",
        "Keeps implementation guidance evidence-backed",
        "Preserves copy-ready formatting"
      ]
    },
    {
      "id": "update-existing-issue",
      "description": "An existing issue draft must be revised after new audit evidence changes scope or dependencies.",
      "category": "positive",
      "trigger": "should-trigger",
      "tags": [
        "update"
      ],
      "expected": [
        "Updates scope and validation to match the new evidence",
        "Keeps earlier intent only when still supported",
        "Makes the delta from the prior draft visible rather than silently rewriting history"
      ]
    },
    {
      "id": "missing-repository-evidence",
      "description": "The request provides a brain dump but no reliable repository evidence for paths, commands, or dependencies.",
      "category": "insufficient-evidence",
      "trigger": "should-trigger",
      "expected": [
        "Does not fabricate exact repository details",
        "Labels assumptions and provisional guidance",
        "Requests or records the missing evidence needed for a precise issue"
      ]
    },
    {
      "id": "bundling-too-much-work",
      "description": "The draft issue mixes several independently shippable outcomes and a migration sequence.",
      "category": "boundary",
      "trigger": "should-trigger",
      "expected": [
        "Splits work into an ordered batch or roadmap when needed",
        "Keeps each issue independently reviewable",
        "Does not hide sequencing risk inside one oversized issue"
      ]
    },
    {
      "id": "implement-instead-negative-trigger",
      "description": "The user wants the code changed now rather than an issue drafted.",
      "category": "negative",
      "trigger": "should-not-trigger",
      "expected": [
        "Declines to substitute issue authoring for immediate implementation",
        "Routes toward implementation work or another execution skill",
        "Avoids returning only issue text when direct changes were requested"
      ],
      "prohibited": [
        "Replacing an explicit implementation request with issue text only"
      ]
    }
  ]
}
